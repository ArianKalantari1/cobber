"""Cobber the Mixologist — a local, deterministic flavour-chemistry MCP server.

The package exposes a small flavour-chemistry engine (:mod:`cobber.engine`) over
hand-seeded data (:mod:`cobber.data`), wrapped in MCP tools (:mod:`cobber.server`).
The server does the maths; the host Claude does the creativity.
"""

__version__ = "0.1.0"
